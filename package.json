{
  "name": "@adeficior/monorepo",
  "packageManager": "bun@1.3.14",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "prepare": "husky && bun run scripts/prepare.ts"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "turbo": "^2.10.8"
  },
  "workspaces": [
    "lib",
    "core",
    "packages/*",
    "modules/*"
  ]
}